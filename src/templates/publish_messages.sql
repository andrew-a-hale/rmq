insert into {name} (
  id,
  message_type,
  payload,
  status,
  priority,
  delay,
  attempts,
  max_attempts,
  inserted_at,
  last_started_at,
  completed_at,
  failed_at
)
select
    %(id)s, 
    %(message_type)s, 
    parse_json(%(payload)s), 
    %(new)s,
    %(priority)s, 
    %(delay)s, 
    0,
    %(max_attempts)s,
    current_timestamp,
    null,
    null,
    null;
